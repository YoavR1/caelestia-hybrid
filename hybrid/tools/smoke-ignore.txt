# smoke-matrix.sh ignore list - reviewed code, not a dumping ground.
#
# One extended-regex per line. A log line matching any of these is not a failure.
# Blank lines and lines starting with # are ignored.
#
# --fix/--baseline can append here, but its output is NOT trustworthy as written: it
# bakes in per-run temp paths and QMLTYPE ids that change every run. Always generalise
# what it appends, and delete anything that is a genuine bug.
#
# Established on CachyOS / Hyprland 0.56.2 / Qt 6.x, nested-compositor run, 2026-09-02.

# =============================================================================
# ENVIRONMENT - absent hardware or services already owned by the host session.
# These are legitimately not our problem inside a throwaway compositor.
# =============================================================================

# No bluetooth adapter in the VM.
Failed to create DBusObjectManagerInterface for "org\.bluez"

# The host session already owns these bus names / agent slots. A second shell
# cannot register them, and correctly says so rather than stealing them.
Could not register notification server at org\.freedesktop\.Notifications
Registration will be attempted again if the active service is unregistered
failed to register listener: GDBus\.Error:org\.freedesktop\.PolicyKit1\.Error\.Failed
failed to register listener on path "/org/quickshell/PolkitAgent"

# Fresh isolated XDG_STATE_HOME genuinely has no colour scheme yet. Writing one is
# the CLI's job and the shell falls back correctly.
Read of .*/state/caelestia/scheme\.json failed: File does not exist

# Two entries lived here suppressing Qt's cross-thread warnings -- "Cannot create children
# for a parent that is in a different thread" and the installEventFilter equivalent --
# attributed to QuickShare's worker thread during teardown. `--audit-ignores` showed both
# matching nothing across 12 runs, and they were silencing a real bug class globally rather
# than one known instance, so they are gone. If cross-thread warnings return, the log will
# name the object and the fix belongs in the code, not here.

# =============================================================================
# The shell fetches weather, GitHub activity and lyrics over the network at startup. A CI
# container has no internet, and even here the weather API returns 503 often enough to have
# failed 5 of 12 runs in one sitting. A boot gate must not depend on someone else's uptime.
#
# This does hide a genuinely malformed request, so if a feature that talks to the network
# stops working, read the log with this file emptied before believing the gate.
caelestia\.requests: \w+: request failed with error

# The user avatar lives at ~/.face by convention and most accounts do not have one. The
# dashboard and lock screen both draw a placeholder icon behind it, so a missing file is
# the normal case rather than a defect. Only reachable once the dashboard is opened, which
# is why it appeared with the interaction pass and not before.
Source file does not exist: .*/\.face

# KNOWN BUGS - real defects, suppressed only so the gate stays useful.
# Each line here is debt. Delete the entry when the bug is fixed, not before.
# =============================================================================

# Upstream's components/Anim.qml reads Tokens.anim.durations, and Anim is a
# NumberAnimation -- not a QQuickItem -- so its attached Tokens can never inherit a
# screen and always resolves against the global layer. Animation durations do not vary
# per monitor, so the value is right; upstream's own diagnostic just cannot tell. Fires
# once per process. Fixing it means changing an upstream file for a cosmetic warning,
# which is the wrong trade (hard rule 6) -- report it upstream instead.
Tokens\.\w+ accessed without a screen set on Anim
